#!/usr/bin/env bash

sudo_args=${@}
command=${1}; shift

case "${command}" in
	"apt")
		case "${1}" in
			"update") /usr/bin/sudo ${sudo_args};;
			*)
				/usr/bin/sudo apt update
				/usr/bin/sudo ${sudo_args}
			;;
		esac
	;;
	*) /usr/bin/sudo ${sudo_args};;
esac
